
export default (t,setHtml) => {
    //t = t.split('–').join('-');
    txtGExp = t
    /*heightFix = 170
    let c = 0
    while(c<t.length){
        if (t[c].charCodeAt(0)===10){
            heightFix += 15
        }
        c++
    }*/
    Preprocess(t,setHtml)
}